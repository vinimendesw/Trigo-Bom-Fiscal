"""Versão única do TrigoBom Fiscal.

Fonte de verdade para o número de versão do app — lida pela UI (ex.: tela
"Sobre") e pelo script de build/empacotamento. Ver CLAUDE.md, seção 14.1.

Segue semver (MAJOR.MINOR.PATCH). Cada bump deve corresponder a uma tag git
(vX.Y.Z) e a uma entrada nova no CHANGELOG.md.
"""

__version__ = "1.0.0"
