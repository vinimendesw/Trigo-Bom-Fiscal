# Ambientes virtuais Python
.venv/
venv/
env/
__pycache__/
*.pyc

# Testes / cache
.pytest_cache/
.coverage
htmlcov/

# Build / distribuição
build/
dist/
*.spec

# Instalador do Tesseract baixado localmente para o build encadeado (~50MB,
# não versionar — cada dev baixa o seu em installer/vendor/, ver CLAUDE.md 14.3)
trigo_bom/installer/vendor/*.exe

# Banco e dados locais (não devem ir para o repositório)
*.db
*.sqlite
*.sqlite3

# Configuração local de pastas do usuário (gerada em runtime, fora do projeto normalmente,
# mas listada por segurança caso apareça aqui)
config.json

# Editor / SO
.vscode/
.idea/
Thumbs.db
.DS_Store
